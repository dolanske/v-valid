// import { defineRule } from '../../core/define'

// export const startsWith = defineRule<{
//   text: string
// }>((value: any, { text }) => {
//   if (typeof value !== 'string')
//     return false

//   return value.startsWith(text)
// })

// // startsWith()
